apiVersion: v1
kind: ConfigMap

metadata:
  name: bank-config

data:
  PORT: "5000"
  DB_NAME: "bank"
  DB_USER: "bank_user"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  POSTGRES_DB : "bank"
  POSTGRES_USER : "bank_user"